# OpenRouter API key (required)
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# GitHub Personal Access Token (required for GitHub PR/repo URLs)
# Create at https://github.com/settings/tokens
# Needs: repo (read) and pull_request (read) scopes
GITHUB_TOKEN=
